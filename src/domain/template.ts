export default interface Template {
    // Atributos template, altere os atributos para os da tabela específica do seu banco de dados
    id: number;
    nome: string; // Insira o nome do atributo e o tipo logo em seguida
    atributo: string 
}